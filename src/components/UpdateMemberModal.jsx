import React, { useState, useEffect } from 'react';
import Modal from 'react-modal';
import Swal from 'sweetalert2';

function UpdateMemberModal({ isOpen, onRequestClose, handleAddOrUpdate, selectedMember }) {
  // const [updateMember, setUpdateMember] = useState({
  //   id_usuario_id: selectedMember.id_usuario_id || '',
  //   id_especialidad_id: selectedMember.id_especialidad_id || '',
  //   id_equipo_id: selectedMember.id_equipo_id || ''
  // });
  
  return (
    <div>UpdateMemberModal</div>
  )
}

export default UpdateMemberModal